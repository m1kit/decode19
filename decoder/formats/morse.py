Pixel = tuple[int, int, int]
PixelGrid = list[list[Pixel]]


def decode(pixels: PixelGrid) -> str:
    """Decode the assignment's morse fixture profile."""
    raise NotImplementedError("morse decoder not implemented")
